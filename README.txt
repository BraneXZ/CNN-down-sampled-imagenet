This is a project that runs CNN to a down sampled 64x64 imagenet. 

It uses ImageNetLabels to label them first and train a model using those labels.

Currently, it the model training does not work properly probably due to the shape of the labels being incorrect. 
